char str[68] = {0};

int m() // 0x080484f4
{
    return printf("%s - %d\n", &str, time(NULL));
}

int main( int argc, char *argv[] )
{
    FILE *stream;

    int32_t *arr1 = malloc(8);
    arr1[0] = 1;
    arr1[1] = malloc(8);

    int32_t *arr2 = malloc(8);
    arr2[0] = 2;
    arr2[1] = malloc(8);

    strcpy(arr1[1], argv[1]);
    strcpy(arr2[1], argv[2]);

    stream = fopen("/home/user/level8/.pass","r");

    fgets(str, 0x44, stream);
    puts("~~");

    return 0;
}
