time_t c;

void m( void )
{
    time(&c);
    printf("%ld\n", c);
}

int main( int argc, char *argv[] )
{
    void *p1 = malloc(8);
    void *p2 = malloc(8);
    void *p3 = malloc(8);
    void *p4 = malloc(8);

    strcpy(p1, argv[1]);
    strcpy(p2, argv[2]);
    fopen(stdin, "r");  // open stdin for reading
    fgets();
    puts();

    return 0;
}
