int m;

void p(char *s)
{
    printf(s);
}

void    n() {
    char buf[520];
    fgets(buf, 520, stdin);
    if (m != 64)
        return;
    p(buf);
    system("/bin/cat /home/user/level5/.pass");
}

int main( void ) {
    n();
    return 0;
}