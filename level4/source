int m;

void p(char *s)
{
    printf(s);
}

void n()
{
    char buf[520];

    fgets(buf, 512, stdin);

    p(buf);

    if (m != 16930116)
        return;

    system("/bin/cat /home/user/level5/.pass");
}

int main( void ) {
    n();
}