int main(int ac, char *av[]) {

    int value = atoi(av[1]);

    if (value != 423) {
        fwrite("No !", 5, 1, stdout);
        return 0;
    }

    char *str = strdup("/bin/sh");
    gid_t egid = getegid();
    uid_t euid = geteuid();

    setresgid(egid, egid, egid);
    setresuid(euid, euid, euid);

    char *const args[] = {str, NULL};

    execv(str, args);
    return 0;
}
