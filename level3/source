int m;

void    v() {

	char	buf[520];

    fgets(buf, 512, stdin);

    printf(buf);

    if (m != 64) {
        return;
	}

    fwrite("Wait what!?\n", 12, 1, stdout);
    system("/bin/sh");
}

int main( void ) {
    v();
}