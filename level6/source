void n( void )
{
	system("/bin/cat /home/user/level7/.pass");
}

void m( void )  // wrong path
{
    puts("Nope");
}

void main( int ac, char **av )
{
    char *buf = malloc(64);
    void (**ptr)(void) = malloc(4);

	*ptr = m;
	
	strcpy(buf, av[1]); // vuln -> strcpy does not check buffer size

	(*ptr)();	// indirect way to call m() -> we need to call n() instead

    return;
}
