int main(int ac, char ** av)
{
    int     nbr;
    char    buf[36];

    nbr = atoi(av[1]);

    if (nbr > 9)
        return (1);

    memcpy(buf, av[2], n);

    if (nbr != $0x574f4c46)
        return (0);

    execl("/bin/sh", "sh", NULL);
    return (0);
}
