int main(int ac, char ** av)
{
    char    buf[36];
	int     nbr;

    nbr = atoi(av[1]);

    if (nbr > 9)
        return (1);

    memcpy(buf, av[2], nbr);

    if (nbr != 0x574f4c46)
        return (0);

    execl("/bin/sh", "sh", NULL);

    return (0);
}
